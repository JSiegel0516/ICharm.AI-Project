import { SettingsIcon } from '@/app/(frontpage)/_components/SettingsIcon';

const SideSettings = () => {
  return (
    <div className="">
      <SettingsIcon size={24} />
    </div>
  );
};

export default SideSettings;
